{
  "hero": {
    "badge": "USER",
    "title": "FRESHNESS",
    "description": "FRESHNESS is deterministic caching — unchanged sources produce identical outputs, skip the work, trust the cache.",
    "cta": [
      {
        "label": "Open FRESHNESS",
        "href": "/MAGIC/TOOLCHAIN/FRESHNESS/",
        "class": "btn-secondary",
        "talk": true
      }
    ]
  },
  "nav": [],
  "fleet": {
    "brand": {
      "mark": "∩",
      "label": "CANONIC",
      "url": "/"
    },
    "sites": [
      {
        "scope": "FOUNDATION",
        "label": "Foundation",
        "url": "https://canonic.org/FOUNDATION"
      },
      {
        "scope": "INDUSTRIES",
        "label": "Industries",
        "url": "https://canonic.org/INDUSTRIES"
      },
      {
        "scope": "MAGIC",
        "label": "MAGIC",
        "url": "https://canonic.org/MAGIC"
      }
    ]
  },
  "sections": [
    {
      "id": "status",
      "eyebrow": "SCOPE",
      "title": "FRESHNESS",
      "dashboard": [
        {
          "metric": {
            "label": "MAGIC",
            "value": "255/255",
            "change": "FULL",
            "trend": "up"
          }
        },
        {
          "metric": {
            "label": "LEARNING",
            "value": "2",
            "change": "patterns",
            "trend": "up"
          }
        }
      ]
    },
    {
      "id": "activity",
      "eyebrow": "LEARNING",
      "title": "Recent Activity",
      "dashboard": [
        {
          "span": 2,
          "table": {
            "headers": [
              "Date",
              "Signal",
              "Pattern"
            ],
            "rows": [
              [
                "2026-03-11",
                "FULL_BUILD_BASELINE",
                "Phase 02 surfaces compilation takes 134s for 306 surfaces — all recompiled regar"
              ],
              [
                "2026-03-11",
                "CONTRACT_FIRST",
                "FRESHNESS declared as TOOLCHAIN contract before any compiler code — gov drives c"
              ]
            ]
          }
        }
      ]
    },
    {
      "id": "canon-transaction",
      "eyebrow": "TRANSACTION",
      "title": "Transaction",
      "description": "``\nGOV source mtimes → freshness manifest → skip/compile decision per surface\nTOOLCHAIN changes → invalidate all → full rebuild\nScope changes → invalidate scope → recompile scope\nMONITORING reports freshness → ops sees stale scopes without building\n``\n\nGovernance drives compilation. Freshness drives speed. Neither overrides the other."
    },
    {
      "id": "canon-source-dependencies",
      "eyebrow": "SOURCE DEPENDENCIES",
      "title": "Source Dependencies",
      "table": {
        "headers": [
          "Surface Type",
          "Sources (relative to GOV scope dir)"
        ],
        "rows": [
          [
            "JEKYLL_DEFAULT",
            "CANON.md, {SCOPE}.md, VOCAB.md, INTEL.md"
          ],
          [
            "JEKYLL_SERVICE",
            "CANON.md, {SCOPE}.md, VOCAB.md, INTEL.md, SHOP.md"
          ],
          [
            "JEKYLL_POST",
            "CANON.md, {slug}.md"
          ],
          [
            "JEKYLL_PAPER",
            "CANON.md, {slug}.md, figures/*"
          ],
          [
            "JEKYLL_BOOK",
            "CANON.md, {slug}.md, cover.*, chapters/*"
          ],
          [
            "JEKYLL_DECK",
            "CANON.md, DECK.md"
          ],
          [
            "JEKYLL_ECON",
            "CANON.md, {SCOPE}.md, econ.json, wallet.json"
          ],
          [
            "JEKYLL_CUSTOM",
            "CANON.md, {SCOPE}.md"
          ]
        ]
      }
    },
    {
      "id": "canon-invalidation-rules",
      "eyebrow": "INVALIDATION RULES",
      "title": "Invalidation Rules",
      "table": {
        "headers": [
          "Trigger",
          "Scope",
          "Behavior"
        ],
        "rows": [
          [
            "TOOLCHAIN/*.json changed",
            "ALL",
            "Full cache clear, full rebuild"
          ],
          [
            "DESIGN theme changed",
            "ALL",
            "Full cache clear, full rebuild"
          ],
          [
            "Scope source file changed",
            "SCOPE",
            "Recompile that scope only"
          ],
          [
            "CANON.md inherits chain changed",
            "SCOPE + children",
            "Recompile scope and descendants"
          ],
          [
            "No source changes",
            "SCOPE",
            "SKIP (cache hit)"
          ]
        ]
      }
    },
    {
      "id": "canon-cache",
      "eyebrow": "CACHE",
      "title": "Cache",
      "table": {
        "headers": [
          "Key",
          "Value"
        ],
        "rows": [
          [
            "location",
            "~/.canonic/.build-cache.json"
          ],
          [
            "format",
            "{ version, toolchain_hash, entries: { scope_key: { sources: {path: mtime}, outputs: [path] } } }"
          ],
          [
            "invalidation",
            "toolchain_hash mismatch → full clear"
          ],
          [
            "CI mode",
            "--no-cache (always full rebuild in CI)"
          ],
          [
            "local mode",
            "--incremental (default for local builds)"
          ]
        ]
      }
    }
  ],
  "_generated": {
    "contract": "/Users/iDrDex/CANONIC/canonic-canonic/MAGIC/TOOLCHAIN/FRESHNESS/FRESHNESS.md",
    "rule": "edit the contract or compiler — not this file"
  },
  "footerTagline": "FRESHNESS | CANONIC",
  "canon": {
    "axiom": "FRESHNESS is deterministic caching — unchanged sources produce identical outputs, skip the work, trust the cache.",
    "constraints": "MUST:     Source dependency rules are declared in FRESHNESS.md — compiler reads contract, not hardcoded lists\nMUST:     TOOLCHAIN hash change invalidates entire cache (full rebuild)\nMUST:     CI always runs --no-cache (correctness is the CI gate, speed is the local gate)\nMUST:     Cache is a runtime artifact at ~/.canonic/.build-cache.json — never in GOV tree\nMUST:     MONITORING deep health reports compile freshness (stale scopes, cache age)\nMUST:     Freshness decisions emit structured output (--- freshness --- / SKIP / COMPILE)\nMUST NOT: Skip validation after incremental build — 255 gate always runs on all outputs\nMUST NOT: Skip GC pass — orphan cleanup always executes regardless of cache\nMUST NOT: Cache stale outputs — missing output files force recompile even if sources unchanged",
    "inherits": "canonic-canonic/MAGIC/TOOLCHAIN",
    "spec": [
      {
        "title": "Transaction",
        "body": "```\nGOV source mtimes → freshness manifest → skip/compile decision per surface\nTOOLCHAIN changes → invalidate all → full rebuild\nScope changes → invalidate scope → recompile scope\nMONITORING reports freshness → ops sees stale scopes without building\n```\n\nGovernance drives compilation. Freshness drives speed. Neither overrides the other.\n\n---"
      },
      {
        "title": "Source Dependencies",
        "body": "| Surface Type | Sources (relative to GOV scope dir) |\n|--------------|-------------------------------------|\n| JEKYLL_DEFAULT | CANON.md, {SCOPE}.md, VOCAB.md, INTEL.md |\n| JEKYLL_SERVICE | CANON.md, {SCOPE}.md, VOCAB.md, INTEL.md, SHOP.md |\n| JEKYLL_POST | CANON.md, {slug}.md |\n| JEKYLL_PAPER | CANON.md, {slug}.md, figures/* |\n| JEKYLL_BOOK | CANON.md, {slug}.md, cover.*, chapters/* |\n| JEKYLL_DECK | CANON.md, DECK.md |\n| JEKYLL_ECON | CANON.md, {SCOPE}.md, econ.json, wallet.json |\n| JEKYLL_CUSTOM | CANON.md, {SCOPE}.md |"
      },
      {
        "title": "Invalidation Rules",
        "body": "| Trigger | Scope | Behavior |\n|---------|-------|----------|\n| TOOLCHAIN/*.json changed | ALL | Full cache clear, full rebuild |\n| DESIGN theme changed | ALL | Full cache clear, full rebuild |\n| Scope source file changed | SCOPE | Recompile that scope only |\n| CANON.md inherits chain changed | SCOPE + children | Recompile scope and descendants |\n| No source changes | SCOPE | SKIP (cache hit) |"
      },
      {
        "title": "Cache",
        "body": "| Key | Value |\n|-----|-------|\n| location | ~/.canonic/.build-cache.json |\n| format | { version, toolchain_hash, entries: { scope_key: { sources: {path: mtime}, outputs: [path] } } } |\n| invalidation | toolchain_hash mismatch → full clear |\n| CI mode | --no-cache (always full rebuild in CI) |\n| local mode | --incremental (default for local builds) |"
      },
      {
        "title": "Constraints",
        "body": "```\nMUST:     Cache location is runtime artifact — never in GOV tree\nMUST:     Freshness check reads FRESHNESS.json for source dependency rules — not hardcoded\nMUST:     TOOLCHAIN hash change invalidates entire cache\nMUST:     CI always builds with --no-cache (correctness gate)\nMUST:     MONITORING deep health reports compile freshness per scope\nMUST:     Cache misses log to structured build output (--- freshness ---)\nMUST:     build-surfaces --incremental prints freshness summary (fresh/compiled/invalidated)\nMUST NOT: Cache override validation — 255 gate still runs on all outputs\nMUST NOT: Skip GC pass — orphan cleanup always executes\nMUST NOT: Hardcode source dependency lists in compiler — read from FRESHNESS.json\n```\n\n---"
      }
    ],
    "intel": [
      {
        "title": "Observability Projection",
        "body": "| Metric | Source | Purpose |\n|--------|--------|---------|\n| cache_hits | build-surfaces --incremental output | Scopes skipped per build (freshness efficiency) |\n| cache_misses | build-surfaces --incremental output | Scopes recompiled per build |\n| build_time_saved_s | Phase 02 timing delta (full vs incremental) | Seconds saved by incremental compilation |\n| stale_scopes | MONITORING deep health freshness block | Scopes needing recompilation (ops visibility) |\n| toolchain_current | MONITORING deep health freshness block | Whether toolchain hash matches cache |\n| top_cache_busters | build-surfaces summary | Source files that trigger the most recompilation |"
      },
      {
        "title": "Evidence",
        "body": "FRESHNESS.json compiled from contract. Build-surfaces reads source dependency rules at runtime. CI always full rebuild. Local defaults to incremental.\n\n---"
      }
    ],
    "learning": [
      {
        "title": "Patterns",
        "body": "| Date | Signal | Pattern | Source |\n|------|--------|---------|--------|\n| 2026-03-11 | FULL_BUILD_BASELINE | Phase 02 surfaces compilation takes 134s for 306 surfaces — all recompiled regardless of changes | build output timing |\n| 2026-03-11 | CONTRACT_FIRST | FRESHNESS declared as TOOLCHAIN contract before any compiler code — gov drives code | FRESHNESS.md |\n\n---"
      }
    ],
    "coverage": "255/255",
    "roadmap": {},
    "vocab": [
      {
        "term": "CACHE",
        "definition": "A JSON manifest mapping scope keys to source mtimes and output paths. Runtime artifact."
      }
    ],
    "chain": [
      {
        "scope": "TOOLCHAIN",
        "axiom": "TOOLCHAIN is code governed by GOV — governance drives code, never the reverse; the boundary between runtime and GOV is absolute.",
        "constraints": "MUST:     magic.c is the single source of dimension truth\nMUST:     magic-heal settles CANON/README/VOCAB/{SCOPE}.md structure — run after content changes\nMUST:     build validates via magic scan before any deploy\nMUST:     build-scopes-json autodiscovers scopes across gov tree + HTTP fleet + magic:// scope\nMUST:     Service->fleet mapping is a direct projection from governed `DEXTER/SERVICES/**/CANON.md` scopes via magic wrappers\nMUST:     Canonical projection artifact is `~/.canonic/SERVICES/GENERATOR/services-manifest.json`\nMUST:     build validates HTTP fleet roots at MAGIC 255 in addition to governance scans\nMUST:     GOV design_language is the only lane ontology source\nMUST:     Fleet frontend git remotes use governed owner mapping from `HTTP.json.sites[].repo_owner`, enforced in deploy preflight\nMUST:     deploy ledgers to LEDGER\nMUST:     Every tool has documented purpose, inputs, outputs\nMUST:     All executables live at ~/.canonic/bin/ — code boundary\nMUST:     All governance lives at ~/CANONIC/ — governance boundary\nMUST:     Fleet sites are discovered from runtime state — no hardcoded site lists in tool scripts\nMUST NOT: Deploy without validation passing\nMUST NOT: Hardcode paths — discover from governance tree\nMUST NOT: Duplicate enforcement — magic.c is the only enforcer, everything else wraps\nMUST NOT: Add parallel scope->fleet mapping layers outside the canonical generated projection\nMUST:     ECON.v1.json and WALLET.v1.json govern vault publish output shape\nMUST:     Every compiled JSON includes _generated: {contract, rule}\nMUST:     vault publish injects _generated into econ.json and wallet.json\nMUST:     CI/CD enforces magic validate 255 on push to main\nMUST NOT: Edit README.md by hand — magic-heal regenerates it\nMUST:     Build pipeline emits structured output (═══ PHASE ═══ / --- step --- / OK/FAIL/WARN)\nMUST:     magic validate output follows structured format (score/255 tier:N STATE)\nMUST:     deploy emits fleet-wide status summary\nMUST:     Error output identifies the failing contract (file path + dimension)\nMUST NOT: Emit unstructured prose in pipeline output\nMUST NOT: Silence failures — every gate produces evidence\nMUST:     Runtime reads CANONIC config from compiled TOOLCHAIN JSON — never inline literals\nMUST:     Worker env vars are REQUIRED in wrangler.toml [vars] — never provide || fallbacks for CANONIC values\nMUST NOT: Swallow exceptions silently — every catch must log or propagate\nMUST:     validate-compliance gate runs in build pipeline — regex scan of banned patterns\nMUST:     magic-tag validates 255 before creating any tag\nMUST:     magic-tag appends to TAGS.md — never edits existing rows\nMUST:     freeze-* tags block deploy from pushing interface contract changes\nMUST:     deploy checks freeze state and warns before pushing during freeze\nMUST:     Tag names follow governed naming convention (semver | freeze-* | patent-filing-*)\nMUST:     TAG.v1.json governs tag record schema\nMUST NOT: Create tags on dirty working tree\nMUST NOT: Delete or move existing tags\nMUST NOT: Deploy breaking interface changes during freeze without explicit --override-freeze\nMUST:     Toolchain dispatch uses STRATEGIES registry — every contract declares `compiler:` in header\nMUST NOT: Hardcode output→function mappings — COMPILERS dict is dead\nMUST:     discover_all() is the single GOV tree walk — galaxy + scopes consume it\nMUST:     classify_kind() reads `kind:` from CANON.md header — no path heuristics\nMUST:     VALID_KINDS = {ORG, SERVICE, USER, VERTICAL, SCOPE, PRINCIPAL} — unknown kind = fail\nMUST:     build-surfaces parses each scope into IR via parse_scope() — emitters consume IR, never re-read .md\nMUST:     Build phases live at build_phases/[0-9][0-9]-* — runner discovers via glob\nMUST:     Each phase sources fleet.conf, exit 0 = pass, non-zero = build stops\nMUST NOT: Inline phase logic in the build runner — each phase is a self-contained script\nMUST:     validate-coin-wiring enforces 5 hard gates: manifest, tier_floor, wallet_existence, shop_resolution, service_crossref\nMUST NOT: Treat COIN violations as warnings — compliant or fail\nMUST:     Every function exists in exactly one file — duplicates deleted, not deprecated\nMUST NOT: Re-export wrappers or compatibility shims for moved functions\nMUST:     build-surfaces reads FRESHNESS.json for source dependency rules — not hardcoded\nMUST:     --incremental is default for local builds, --no-cache for CI\nMUST:     Cache is runtime artifact at path declared in FRESHNESS.json — never in GOV tree\nMUST NOT: Skip validation after incremental build — 255 gate always runs\nMUST NOT: magic, magic-heal, build, build-scopes-json, deploy, install-hooks — governance drives code, the reverse"
      },
      {
        "scope": "MAGIC",
        "axiom": "MAGIC is the enforcement layer — three primitives, one governed economy, COMPLIANCE as the product.",
        "constraints": "MUST:     CANON.md in every scope\nMUST:     Services compose primitives — never duplicate\nMUST:     Primitive structure is fixed — industry is the only variable\nMUST:     Primitives compose into services — never duplicate\nMUST:     Services connect through SHOP.md and VAULT.md projection files\nMUST:     SHOP.md = public projection file (filesystem-discoverable, UPPERCASE per LANGUAGE)\nMUST:     VAULT.md = private projection file (filesystem-discoverable, auth-gated, UPPERCASE per LANGUAGE)\nMUST:     Instance = service projected through user governance context\nMUST:     Instance directories live at USER scope ({USER}/{PLURAL}/), not nested in SERVICES/\nMUST:     Service directories (SERVICES/{SINGULAR}/) define schemas — instances hold content\nMUST:     Every .md compiles to .json with the same name (direct mapping)\nMUST:     CANON.md = axiom + universal constraints only (no service names, no paths, no implementation)\nMUST:     README.md = how to run the CANON only\nMUST:     {SCOPE}.md = SPEC — the interface (purpose, routes, projections, ecosystem)\nMUST NOT: Hardcode service names in CANON constraints (law speaks universals)\nMUST:     Inheritance resolves upward — scopes compose by directories\nMUST:     Tier algebra is canonical — DESIGN.md is the single source (COMPLIANCE tier algebra)\nMUST NOT: Expose dimension internals to users or developers\nMUST NOT: Hardcode outside governed contracts\nMUST:     Nonprofits get enterprise for free\nMUST:     ORG is the container; USER is the repo (`github.com/{org}/{user}`; duplicates across orgs allowed)\nMUST:     MARKET/ SALES/ GTM/ exist (META self-closure; one primitive each)\nMUST:     Each META sub-scope maps exactly one primitive (INTEL, CHAT, COIN)\nMUST NOT: Add META business knowledge outside MAGIC/ scope\nMUST NOT: Remove META sub-scope without replacing its primitive coverage\nMUST:     `{SCOPE}.md` is the scope contract surface; it MUST NOT be treated as a generic filename placeholder\nMUST:     LEARNING.md is the terminal — governance evidence, patterns, epoch rotation\nMUST:     LEARNING/ is the IDF directory — machine-generated individual data files\nMUST:     LEARNING.md rotates at epoch boundaries — frozen epochs archive as LEARNING-{EPOCH}.md at scope root\nMUST:     LEARNING.md is always the current epoch — active, append-only\nMUST:     Epoch boundary = EVOLUTION signal in LEARNING.md (named, dated, sourced)\nMUST NOT: Delete archived LEARNING epochs — append-only history\nMUST:     MAGIC defines the triad interface directly:\nMUST:     COMPLIANCE/ + GALAXY/ + SURFACE/\nMUST NOT: Define conflicting tier algebra in downstream scopes; downstream must inherit this contract\nMUST: New constraint: MUST NOT: Expose private kernel internals\nMUST: New constraint: MUST: Declare governance.\nMUST: New constraint: COVERAGE.md MUST be generated by build-surfaces — not hand-authored\nMUST: New constraint: README.md MUST be lean (scope + inherits + axiom) — no boilerplate"
      },
      {
        "scope": "FOUNDATION",
        "axiom": "FOUNDATION defines the LANGUAGE — three axioms, compiler correspondence, the paper proves itself.",
        "constraints": "MUST:     LANGUAGE defines all governance primitives\nMUST:     Every scope inherits from FOUNDATION\nMUST:     Triad (CANON.md + VOCAB.md + README.md) in every scope\nMUST NOT: Define terms outside VOCAB.md\nMUST NOT: Hardcode outside the kernel\nSHOULD:   Vocabulary closure — every term resolves to a definition\nMUST: New constraint: MUST: Surface FOUNDATION thesis, axioms, and programming correspondence from governed content\nMUST: New constraint: MUST: Declare governance.\nMUST: TRIAD, INHERITANCE, INTROSPECTION — every scope must satisfy all three\nMUST: TRIAD, INHERITANCE, INTROSPECTION — every tier must satisfy all three\nMUST: TRIAD, INHERITANCE, INTROSPECTION — every service must validate against these axioms"
      }
    ]
  }
}
