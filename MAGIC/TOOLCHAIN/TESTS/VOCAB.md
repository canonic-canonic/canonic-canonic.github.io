# TESTS — VOCAB

inherits: canonic-canonic/MAGIC/TOOLCHAIN/TESTS

---

| Term | Definition |
|------|-----------|
| Fixture | A self-contained minimal scope used as compiler input |
| Expected output | The reference CANON.json that the compiler must produce for a given fixture |
| Volatile field | A JSON field that changes between runs (timestamps, paths) and must be stripped before comparison |
| Test runner | The script that discovers fixtures, compiles them, and diffs against expected output |

---

*TESTS | VOCAB | TOOLCHAIN*
<!-- _generated: build-surfaces -->
