---
layout: default
scope: bin
title: "bin"
description: "bin is three engines, three tiers — each axiom self-closes, and together they equal 255 = MAGIC."
footerTagline: "bin"
accent: "#00ff88"
accent_rgb: "0, 255, 136"
talk: side
view: web
views:
  - gov
  - web
  - tex
pdf: /MAGIC/TOOLCHAIN/bin/bin.pdf
downloads:
  - label: "PDF"
    href: "/MAGIC/TOOLCHAIN/bin/bin.pdf"
hero:
  badge: bin
  title: "bin"
  description: "bin is three engines, three tiers — each axiom self-closes, and together they equal 255 = MAGIC."
  cta:
    - label: "Open bin"
      href: /MAGIC/TOOLCHAIN/bin/
      class: btn-secondary
      talk: true
---
<!-- _generated: build-surfaces -->

- **THREE_ENGINES_THREE_TIERS** — three engines map to three tiers; each axiom self-closes with no infinite recursion and no external dependency.
- **EVERY_EXECUTABLE_GOVERNED** — every executable is documented with purpose, inputs, and outputs; `magic` is the single source of dimension truth.
- **BUILD_DEPLOY_LEDGERED** — every build validates via `magic scan` (255 or reject) and every deploy ledgers to LEDGER.
## Constraints

```
MUST:     Every executable governed — documented purpose + inputs + outputs
MUST:     Every build validates via magic scan — 255 or reject
MUST:     Every deploy ledgers to LEDGER
MUST:     magic binary is the single source of dimension truth
MUST:     Each engine self-closes — no infinite recursion, no external dependency
MUST NOT: Hardcode paths — discover from governance tree
MUST NOT: Deploy without validation passing
```

---

*bin | CANON | TOOLCHAIN*
