# APPLE — ROADMAP

inherits: .

---

## Now (2026-02)
- Establish iOS project scaffold: native rendering shell that reads governed config at runtime (zero baked constants)
- Wire `magic validate` into Xcode build pipeline: iOS builds fail if governance score drops below 255
- Delegate validation and scoring to governed truth: no local policy forks for provider lists, thresholds, or lane maps

## Next (2026-Q2)
- Ship native TALK surface: iOS CHAT interface wired to INTEL with governed disclaimers and session ledger
- Implement governed config sync: iOS app pulls CANON.json, fleet.yml, and SURFACE.json from GOV endpoints at launch
- Build native DESIGN token consumption: iOS views derive from the same Sass token values as web JEKYLL theme

## Later
- Full SERVICES parity: iOS surface projects INTEL, CHAT, COIN, and LEDGER with identical governance to web
- App Store compliance gate: `magic validate` + native integration checks required before any release submission
- Offline-first governed rendering: iOS app caches GOV state and renders deterministically without network

---

*APPLE | ROADMAP | CANONIC*
<!-- _generated: build-surfaces -->
