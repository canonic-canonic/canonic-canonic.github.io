# VOCAB

inherits: canonic-canonic/MAGIC/SURFACE/VOCAB.md

| Term | Definition |
|------|-----------|
| CONTROLS | Unified control interface — five dimensions per surface type. Every control acts on content within context. |
| TALK | Control dimension — overlay position (top or side). TALK.open() / TALK.close(). |
| DOWNLOAD | Control dimension — downloadable asset types (pdf, ppt, word, or none). Direct href. |
| VIEW | Control dimension — default content rendering mode (gov, web, tex). CONTROLS.viewTo(). |
| VIEWS | Control dimension — available view set. GOV is universal. Toggle bar when >1. |
| GATE | Control dimension — content gate type (contribute or none). Renders identity gate + nav anchor. |
| SURFACE_TYPE | Content category — compiler reads defaults from SURFACE.json. |

---

*VOCAB | CONTROLS*
<!-- _generated: build-surfaces -->
