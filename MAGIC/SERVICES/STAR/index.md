---
layout: default
scope: STAR
title: "STAR"
description: "STAR is your personal portal in the GALAXY — INTEL + CHAT + COIN composed into a unified personal surface, TIMELINE joins the primitives."
footerTagline: "STAR"
accent: "#00ff88"
accent_rgb: "0, 255, 136"
talk: side
view: web
views:
  - gov
  - web
  - tex
pdf: /MAGIC/SERVICES/STAR/star.pdf
downloads:
  - label: "PDF"
    href: "/MAGIC/SERVICES/STAR/star.pdf"
hero:
  badge: STAR
  title: "STAR"
  description: "STAR is your personal portal in the GALAXY — INTEL + CHAT + COIN composed into a unified personal surface, TIMELINE joins the primitives."
  cta:
    - label: "Open STAR"
      href: /MAGIC/SERVICES/STAR/
      class: btn-secondary
      talk: true
---
<!-- _generated: build-surfaces -->

- **STAR_IS_PERSONAL_CLOUD** — STAR is the governed personal cloud; GALAXY shows the organizational topology, STAR shows the personal topology, and every USER gets a STAR.
- **PRIMITIVES_COMPOSED** — STAR composes all three primitives (INTEL + CHAT + COIN) and discovers the USER's GOV tree via `magic scan`, never hardcoded.
- **TIMELINE_IS_JOIN** — TIMELINE is a read-only JOIN across existing event sources and never duplicates; the envelope is compatible with LEDGER .idf and VAULT TIMELINE.jsonl.
- **PERSONAL_DATA_AUTH_GATED** — VAULT, WALLET, NOTIFIER, and CALENDAR are auth-gated; the GOV lane stays public because compliance rings are the product.
## Constraints

```
MUST:     Compose all three primitives (INTEL + CHAT + COIN)
MUST:     Discover USER's GOV tree via magic scan — never hardcode
MUST:     TIMELINE is a read-only JOIN across existing event sources — never duplicate
MUST:     TIMELINE envelope compatible with LEDGER .idf and VAULT TIMELINE.jsonl
MUST:     Every lane WIRES to existing services — never duplicates
MUST:     Auth-gate all personal data (VAULT, WALLET, NOTIFIER, CALENDAR)
MUST:     GOV lane is public (compliance rings are the product)
MUST:     Discover services via magic scan (TALKS/*/CANON.md, SERVICES/*/CANON.md)
MUST:     Aggregate LEARNING patterns from all user scopes
MUST:     Surface compliance ring (8 MAGIC dimensions) in portal HUD
MUST:     Publish STAR-TIMELINE.json via build pipeline (deterministic, rerun-safe)
MUST:     NETWORK service — declare routes on api.canonic.org
MUST NOT: Write to source event streams (VAULT, LEDGER, NOTIFIER are read-only)
MUST NOT: Duplicate WALLET, SHOP, VITAE, or LEARNING surfaces
MUST NOT: Hardcode lane sources — discover via GOV tree walk
MUST NOT: Expose private data (VAULT, CALENDAR, NOTIFIER) without auth
```

---

*STAR | CANON | SERVICES*
