# MAGIC — CANON

inherits: canonic-canonic/FOUNDATION

---

## Axiom

**MAGIC within FOUNDATION renders the governance galaxy as an interactive visualization.**

This nested scope composes FOUNDATION language with MAGIC runtime to display the full scope tree.

---

## Constraints

```
MUST:     Render Galaxy visualization from governed scopes.json
MUST:     Inherit visual identity from parent FOUNDATION scope
MUST:     Load scope data from MAGIC/scopes.json via governed path
MUST NOT: Hardcode scope labels, bits, or hierarchy
MUST NOT: Expose kernel internals in the visualization
SHOULD:   Keep Galaxy rendering consistent with MAGIC scope galaxy
```

---

*MAGIC | CANON | FOUNDATION-WEB*
