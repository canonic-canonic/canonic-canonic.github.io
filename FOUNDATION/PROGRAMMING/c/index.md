---
layout: default
scope: c
title: "c"
description: "C is the kernel runtime — bare-metal compliance and the universal FFI bridge every other runtime crosses."
footerTagline: "c"
accent: "#00ff88"
accent_rgb: "0, 255, 136"
talk: side
view: web
views:
  - gov
  - web
  - tex
pdf: /FOUNDATION/PROGRAMMING/c/c.pdf
downloads:
  - label: "PDF"
    href: "/FOUNDATION/PROGRAMMING/c/c.pdf"
hero:
  badge: c
  title: "c"
  description: "C is the kernel runtime — bare-metal compliance and the universal FFI bridge every other runtime crosses."
  cta:
    - label: "Open c"
      href: /FOUNDATION/PROGRAMMING/c/
      class: btn-secondary
      talk: true
---
<!-- _generated: build-surfaces -->

- **FFI_IS_KERNEL** — the C implementation is the foreign-function-interface kernel: bare metal, no abstraction tax.
- **C_ABI_IS_UNIVERSAL_BRIDGE** — the C ABI is the universal bridge between language runtimes, exported as FFI bindings for all peers.
- **NEOFUNCTIONALIZED_FROM_RUST** — C is neofunctionalized from Rust for universal FFI, preserving the full compliance specification.
## Constraints

1. C MUST validate against SPEC.
2. C MUST implement the full compliance specification.
3. C MUST provide FFI bindings for other runtimes.
4. C SHOULD enable kernel-level compliance.

---

*CANONIC-C | CANON | PROGRAMMING*
